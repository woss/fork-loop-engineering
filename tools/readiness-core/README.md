# @cobusgreyling/readiness-core

Core types and utilities for `loop-audit` and `goal-audit`.

## Usage

This package is intended for internal use within the `loop-engineering` monorepo. It exports shared types like `BaseAuditResult`, `Finding`, and core heuristics for auditing file existence and skill directories.
